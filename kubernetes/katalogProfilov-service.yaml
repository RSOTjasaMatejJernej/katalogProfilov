apiVersion: v1
kind: Service
metadata:
  name: katalog-service
  labels:
    app: katalog-profilov
spec:
  type: NodePort
  ports:
  - port: 8081
    protocol: TCP
    targetPort: 8081
  selector:
    app: katalog-profilov